import { Server } from 'socket.io'; // Import the Server class from the 'socket.io' library for WebSocket communication
import http from 'http'; // Import the 'http' module to create an HTTP server
import express from 'express'; // Import the 'express' framework for building the server

// Create an instance of an Express application
const app = express();
